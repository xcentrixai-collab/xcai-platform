import { PageHeader } from '@/components/page-header';
import { SectionWrapper } from '@/components/section-wrapper';

export default function ContactPage() {
  return (
    <SectionWrapper>
      <PageHeader
        eyebrow="Connect"
        title="Contact X Centrix AI"
        description="Reach out to discuss partnerships, implementation support, and enterprise deployment."
      />
    </SectionWrapper>
  );
}
