import { PageHeader } from '@/components/page-header';
import { SectionWrapper } from '@/components/section-wrapper';

export default function StartTrialPage() {
  return (
    <SectionWrapper>
      <PageHeader
        eyebrow="Trial"
        title="Start Your 7-Day Trial"
        description="Activate your workspace and evaluate the X Centrix AI platform in a guided onboarding experience."
      />
    </SectionWrapper>
  );
}
